import SignUp from "../components/auth/sign_up";
import Layout from "../components/layout";

const Index = () => {
  return (
    <Layout>
      <SignUp />
    </Layout>
  );
};

export default Index;
